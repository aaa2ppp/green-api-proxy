# Адрес и порт сервера (по умолчанию :8087)
#SERVER_ADDR=:8087

# Включить TLS (yes/no, по умолчанию no)
#SERVER_TLS=no

# Разрешённый Origin для CORS (пусто = CORS запрещён)
#ALLOW_ORIGIN=

# Базовый URL Green API (по умолчанию https://api.green-api.com)
#GREEN_API_BASE_URL=https://api.green-api.com

# Уровень логирования (DEBUG-1, DEBUG, INFO, WARN, ERROR)
#LOG_LEVEL=INFO

# Таймаут на чтение запроса от клиента (по умолчанию 1s)
#READ_TIMEOUT=1s

# Таймаут на обработку и ответ клиенту (по умолчанию 4s)
#WRITE_TIMEOUT=4s

# Таймаут на выполнение прокси-запроса к Green API (по умолчанию 2s)
#REDIRECT_TIMEOUT=2s
